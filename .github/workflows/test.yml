name: tests
on: [push, pull_request]

jobs:
  host-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and run host tests
        run: |
          gcc -std=c11 -Wall -Wextra -Werror -Icore/include \
              test/test_apogee.c core/src/apogee.c core/src/vapogee.c \
              -lm -o test_apogee
          ./test_apogee